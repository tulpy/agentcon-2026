Fixtures are constructed in-memory in `test_discover.py` to keep the test
suite hermetic and avoid drift with the live Azure Policy REST shape. Leave
this directory for future per-tenant captures if needed.
