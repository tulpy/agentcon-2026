# ─── Docker Image Build & Push ─────────────────────────────────
# Read automatically by `docker compose` — see docker-compose.yml.
# Publish:  docker compose build --push   (linux/amd64 + linux/arm64)

# Docker registry URL (e.g., docker.io/myusername, ghcr.io/myorg, myacr.azurecr.io)
REGISTRY=docker.io/myusername

# Image name (repository path under the registry)
IMAGE_NAME=drawio-mcp-server

# Semantic version for image tags (e.g., 3.0.1). Also tagged as `latest`.
IMAGE_VERSION=latest

# ─── MCP Server Configuration ──────────────────────────────────
# These can also be set via CLI flags (CLI takes precedence).

# Transport type: stdio, http, or stdio,http (default: stdio)
# CLI equivalent: --transport <type>
TRANSPORT=stdio

# HTTP server port (default: 8080)
# CLI equivalent: --http-port <number>
HTTP_PORT=8080

# Logger type: console or mcp_server (default: console)
LOGGER_TYPE=console

# Path to Azure public service icons XML library (auto-detected if unset)
# AZURE_ICON_LIBRARY_PATH=./assets/azure-public-service-icons/000 all azure public service icons.xml

# ─── Development / Debugging ───────────────────────────────────
# ⚠️ DEV MODE ONLY — NOT FOR PRODUCTION USE

# Auto-save diagram XML to ./diagrams/ folder (default: disabled)
# When enabled, export-diagram and finish-diagram tools will save their output
# to timestamped .drawio files in the diagrams/ directory for debugging.
# Accepts: true, 1 (enabled) | any other value (disabled)
# SAVE_DIAGRAMS=true
