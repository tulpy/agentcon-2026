---
description: "Good prompt: uses generic agent: agent and correctly declares an explicit model:."
agent: agent
model: "GPT-5.5"
---

# Good Generic-Agent Prompt Fixture

This fixture uses `agent: agent` (generic) and declares an explicit
`model:`. The validator should produce zero findings under
`prompt-model-source-001`.
